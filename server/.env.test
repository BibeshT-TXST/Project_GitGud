# ----- .env.test -----
# Test-only environment variables.
# These are dummy values used by Jest — never production secrets.
# dotenv loads this file at the top of app.js via require('dotenv').config().

DB_USER=testuser
DB_PASSWORD=testpass
DB_HOST=localhost
DB_PORT=5432
DB_NAME=testdb
JWT_SECRET=test-jwt-secret
PEPPER_SECRET=test-pepper-secret

